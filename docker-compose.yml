networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
    depends_on:
      - host-app
    networks:
      - dev

  host-app:
    build:
      context: ./host
    networks:
      - dev